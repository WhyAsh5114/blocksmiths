{
  "name": "prediction-market-contracts",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:projectcoin": "hardhat ignition deploy ignition/modules/ProjectCoin.ts",
    "deploy:factory": "hardhat ignition deploy ignition/modules/ProjectCoinFactory.ts",
    "clean": "hardhat clean"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^3.0.0",
    "dotenv": "^16.4.7",
    "hardhat": "^2.22.18"
  }
}
